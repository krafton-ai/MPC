#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name = 'gpt3chat',
    version='0.1.0',
    description='Modular GPT3 Chatbot Package',
    author='',
    packages = find_packages(),
)
